require 'spec_helper'

describe HttpActivity do
  it 'has a version number' do
    expect(HttpActivity::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
